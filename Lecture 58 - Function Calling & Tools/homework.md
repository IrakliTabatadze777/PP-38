# ამოცანა 1 — ახალი tool-ების შექმნა და schema-ები

შექმენი ორი ახალი tool:

- `calculate` — იღებს მათემატიკურ გამოთქმას სტრინგის სახით და აბრუნებს შედეგს. გამოიყენე `eval` შემდეგნაირად:

```python
def calculate(expression: str):
    try:
        result = eval(expression)
        return {"result": result}
    except Exception as e:
        return {"error": str(e)}
```

- `get_currency_rate` — იღებს ორ ვალუტის კოდს და აბრუნებს mock გაცვლის კურსს. მაგალითი:

```python
def get_currency_rate(from_currency: str, to_currency: str):
    rates = {
        ("USD", "GEL"): 2.71,
        ("EUR", "USD"): 1.08,
        ("EUR", "GEL"): 2.92,
        ("USD", "EUR"): 0.93,
    }
    rate = rates.get((from_currency.upper(), to_currency.upper()))
    if rate is None:
        return {"error": "კურსი ვერ მოიძებნა"}
    return {"rate": rate}
```

დაწერე ორივე tool-ის სრული function declaration schema (name, description, parameters, required ველებით).  
შექმენი mapping ფუნქცია, რომელიც tool-ის სახელის მიხედვით იძახებს შესაბამის ფუნქციას და გადასცემს არგუმენტებს.

შემდეგ გაუშვი მოდელი შემდეგი კითხვებით (თითოეული ცალკე):

1. გამოთვალე 27 * 14.
2. რა არის 125 გაყოფილი 5-ზე.
3. მითხარი რა კურსია ევროს დოლართან?“
4. გამოთვალე (18 + 7) * 3

დაამუშავე ყველა function_call სწორად და გამოიტანე საბოლოო პასუხები.

# ამოცანა 2 — ბევრი tool-ის ერთად გამოყენება 

გააფართოვე წინა ამოცანის კოდი ისე, რომ:

- ერთ კითხვაზე მოდელმა შეძლოს რამდენიმე tool-ის ერთდროული ან თანმიმდევრული გამოძახება;
- ყველა მიღებული function_response ერთად გაიგზავნოს follow-up მოთხოვნაში;
- თუ მოდელი გამოიძახებს tool-ს, რომელიც mapping-ში არ არსებობს, დააბრუნო ნათელი შეცდომის შეტყობინება და მაინც გააგრძელო საუბარი.

გატესტე შემდეგი კითხვებით:

1. „გამოთვალე 45 * 8 და ამავე დროს მითხარი რა კურსია დოლარის ლართან და ევროს დოლართან“
2. „რა არის 1000 გაყოფილი 25-ზე? ასევე მითხარი დღევანდელი კურსი USD-GEL“
3. „გამოთვალე 9 ** 3 და მითხარი რა ღირს 50 ევრო ლარში“